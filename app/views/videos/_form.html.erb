<%= simple_form_for(video) do |f| %>

  <%= f.input :name_en %>
  <%= f.input :name_ar %>
  <%= f.input :youtube_id %>
  <%= f.association :category %>

  <%= f.submit :class => "btn btn-primary" %> 
<% end %>
